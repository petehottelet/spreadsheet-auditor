# Development and CI dependencies.
#
# NOTE: pyproject.toml's [project.optional-dependencies].dev is the canonical
# dev dependency set (`pip install -e .[dev]`). This file mirrors it for
# `pip install -r` workflows; keep the two in sync.
-r requirements.txt

pytest>=8.0
jsonschema>=4.0

# Optional runtime extras exercised by CI:
defusedxml>=0.7
networkx>=3.0
PyYAML>=6.0
